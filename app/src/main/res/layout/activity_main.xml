<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background = "@color/layout"
    android:orientation="vertical"
    >

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginTop="4dp"
        >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:layout_marginStart="12dp"

            android:gravity="top"

            android:text = "@string/title"
            android:textColor="@color/white"
            android:textSize="@dimen/textSize"
            />
    </FrameLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"

        android:orientation="vertical"
        >
        <Button
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginBottom="@dimen/buttonBetween"
            android:layout_marginTop="@dimen/buttonMarginTopBottom"
            android:layout_marginStart="@dimen/buttonMarginLeft"
            android:layout_marginEnd="@dimen/buttonMarginRight"
            android:layout_weight="1"

            android:gravity="center_horizontal|center_vertical"

            app:icon = "@drawable/search"
            app:iconGravity = "textStart"
            app:iconPadding = "@dimen/betweenIconText"
            app:iconTint = "@color/button_text"
            app:cornerRadius = "@dimen/buttonCornerRadius"

            android:backgroundTint="@color/white"

            android:text="@string/search"
            android:textColor="@color/button_text"
            android:textSize="@dimen/textSize"
            android:textAllCaps="false"/>

        <Button
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginBottom="@dimen/buttonBetween"
            android:layout_marginTop="@dimen/buttonBetween"
            android:layout_marginStart="@dimen/buttonMarginLeft"
            android:layout_marginEnd="@dimen/buttonMarginRight"
            android:layout_weight="1"

            android:gravity="center_horizontal|center_vertical"

            app:icon = "@drawable/media"
            app:iconGravity = "textStart"
            app:iconPadding = "@dimen/betweenIconText"
            app:iconTint = "@color/button_text"
            app:cornerRadius = "@dimen/buttonCornerRadius"

            android:backgroundTint="@color/white"

            android:text="@string/media"
            android:textColor="@color/button_text"
            android:textSize="@dimen/textSize"
            android:textAllCaps="false"/>

        <Button
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginBottom="@dimen/buttonMarginTopBottom"
            android:layout_marginTop="@dimen/buttonBetween"
            android:layout_marginStart="@dimen/buttonMarginLeft"
            android:layout_marginEnd="@dimen/buttonMarginRight"
            android:layout_weight="1"

            android:gravity="center_horizontal|center_vertical"

            app:icon = "@drawable/settings"
            app:iconGravity = "textStart"
            app:iconPadding = "@dimen/betweenIconText"
            app:iconTint = "@color/button_text"
            app:cornerRadius = "@dimen/buttonCornerRadius"

            android:backgroundTint="@color/white"

            android:text="@string/settings"
            android:textColor="@color/button_text"
            android:textSize="@dimen/textSize"
            android:textAllCaps="false"/>

    </LinearLayout>

</LinearLayout>